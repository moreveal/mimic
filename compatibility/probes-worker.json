[
  {
    "name": "dedicated worker realm surface",
    "expression": "new Promise((resolve,reject)=>{const source=`onmessage=()=>{try{const chain=[];for(let value=self;value&&chain.length<8;value=Object.getPrototypeOf(value))chain.push({tag:Object.prototype.toString.call(value),ctor:value.constructor&&value.constructor.name,names:Object.getOwnPropertyNames(value).sort()});const slash=location.href.lastIndexOf('/');postMessage({same:self===globalThis,chain,types:{window:typeof window,document:typeof document,WorkerGlobalScope:typeof WorkerGlobalScope,DedicatedWorkerGlobalScope:typeof DedicatedWorkerGlobalScope,Blob:typeof Blob,URL:typeof URL,crypto:typeof crypto,performance:typeof performance,ReadableStream:typeof ReadableStream,fetch:typeof fetch,location:typeof location,navigator:typeof navigator},navigator:typeof navigator==='object'?{userAgent:navigator.userAgent,platform:navigator.platform,language:navigator.language,languages:Array.from(navigator.languages||[]),hardwareConcurrency:navigator.hardwareConcurrency,deviceMemory:navigator.deviceMemory}:null,location:typeof location==='object'?{hrefPrefix:location.href.slice(0,slash+1),origin:location.origin,protocol:location.protocol,host:location.host,pathnamePrefix:location.pathname.slice(0,location.pathname.lastIndexOf('/')+1)}:null,crypto:typeof crypto==='object'?{tag:Object.prototype.toString.call(crypto),subtle:typeof crypto.subtle,getRandomValues:typeof crypto.getRandomValues,randomUUID:typeof crypto.randomUUID}:null,performance:typeof performance==='object'?{tag:Object.prototype.toString.call(performance),now:typeof performance.now,timeOriginType:typeof performance.timeOrigin}:null})}catch(error){postMessage({error:String(error),stack:error&&error.stack})}}`;const url=URL.createObjectURL(new Blob([source],{type:'text/javascript'}));const worker=new Worker(url);const timer=setTimeout(()=>{worker.terminate();URL.revokeObjectURL(url);reject(new Error('worker probe timeout'))},5000);worker.onmessage=event=>{clearTimeout(timer);worker.terminate();URL.revokeObjectURL(url);resolve(event.data)};worker.onerror=event=>{clearTimeout(timer);worker.terminate();URL.revokeObjectURL(url);reject(new Error(event.message||'worker error'))};worker.postMessage(null)})"
  },
  {
    "name": "dedicated worker scheduling",
    "expression": "new Promise((resolve,reject)=>{const source=`onmessage=()=>{const order=[];Promise.resolve().then(()=>order.push('microtask'));setTimeout(()=>{order.push('timer');postMessage(order)},0);order.push('task-end')}`;const url=URL.createObjectURL(new Blob([source],{type:'text/javascript'}));const worker=new Worker(url);const timer=setTimeout(()=>{worker.terminate();URL.revokeObjectURL(url);reject(new Error('worker scheduling timeout'))},5000);worker.onmessage=event=>{clearTimeout(timer);worker.terminate();URL.revokeObjectURL(url);resolve(event.data)};worker.onerror=event=>{clearTimeout(timer);worker.terminate();URL.revokeObjectURL(url);reject(new Error(event.message||'worker error'))};worker.postMessage(null)})"
  }
]
