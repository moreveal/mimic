# Media MIME and capability observations — 2026-09-12

HDEX5/Aqcaj8/XqEQ3 were generated by canPlayType, MediaSource.isTypeSupported and
MediaCapabilities.decodingInfo. The missing MIME operations and one-string
content-type whitelist could not represent container/codec grammar or distinguish
ambiguous types from precise configurations. A shared profile format catalog and
codec parser now drive all three Window projections. The obsolete independent
decoding-content-type whitelist was removed.

The parser separates MIME container, codec family/profile and API use: a bare
container can be `maybe` without being a supported MediaSource configuration;
legacy VP9 labels can play while precise capability queries need profile details;
a MediaCapabilities track describes one codec, unlike a multiplexed media file.
RTP mode queries reuse the RTP codec catalog. Capability configuration conversion
and observed errors are reproduced, including video dimensions and numeric
framerate strings. Fresh result objects retain supported/smooth/powerEfficient and
keySystemAccess property shape.

Availability and hardware-efficiency predictions are frozen environment policy.
The profile reports efficient AVC/HEVC/VP9/AV1 decoding and software VP8 behavior;
no native decoder, GPU, OS media service or fabricated media output is required.
This does not implement streaming SourceBuffer playback or encrypted-media key
systems, and does not claim every codec-profile grammar edge has been measured.

Fresh headful Chrome 152.0.7977.82 A/B controls and fresh production have zero
differing leaves for the committed media_support oracle: MIME variants across
MP4/WebM/Ogg/WAV/MPEG/AAC/FLAC, malformed/ambiguous codec strings, file and
media-source decoding, WebRTC encoding and conversion errors. The original
045102 configurations were independently extracted with offline instrumentation:
audio stereo 128 kbps at 48 kHz and nine 1920x1080 video configurations with
2646242 bitrate and string framerate `25`. These inputs are included in the
minimal frozen oracle. Private instrumentation is diagnostic evidence only;
the clean minimal headful A/B runs own the reference expectations.

Passed:

```
go test ./internal/browser -run 'TestMediaSupport|TestRTP' -count=1
go test ./internal/browser -run 'TestNavigatorCapturedShape|TestNavigatorCapabilityDomains|TestNavigatorSecureExposure|TestMediaElement' -count=1
```

The first includes ordinary/restored-bootstrap cases. Full raw A/B/production
receipts, runner and measured binary are outside the worktree at
`E:/GitHub/mimic/.build/residual-media-delegated/support`; original-operation
extracts are in `diagnostic-inputs` and `diagnostic-audio`. No full suite, race or
performance gate was run. Parent full-field replay determines any residual scope
beyond this measured Window MIME contract.
