# Manual 045102 semantic closure

Reference: frozen Chrome 152.0.7977.82. Input capture:
`compatibility/private-captures/manual-20260912-045102`.

Two fresh offline replays each produced ten complete matched payload pairs with
zero parse errors. The final registry contains all 1,499 rows: 1,117 equal rows
and 382 different rows across 54 fields. Row equality is not the completion
criterion because the payload includes clocks, random values, selected
environment state, response observations and intentionally bounded synthetic
graphics projections.

Known fixable semantic or control-flow residual: **0**. No new stable semantic
difference absent from the reviewed registry was found. The row-level evidence
is retained in `manual-045102-field-registry.csv`; producer classifications and
the reason for each remaining field are in
`manual-045102-field-notes.json`.

The remaining stable boundaries are:

- WebGL extension inventory (`qydV6`, `OYbs6`): every advertised Mimic
  extension has an operation-level frozen oracle. Chrome's remaining 15 WebGL1
  and 19 WebGL2 names require unsupported shader/raster contracts and are not
  advertised as empty objects.
- Synthetic geometry and graphics (`HPcn5`, `XYvy9`, `gAtz0`, `auHG6`,
  `zfym0`): ownership, lifecycle, color state, transforms, zero-area boxes and
  continuations are fixed. Remaining values are bounded glyph/raster/layout
  edge precision from the coherent software observation model.
- Selected profile and environment (`Jpzg5`, `etmnR7`, `lDUiR4`, `maNnU6`,
  `xWWV8`, `XCvwf5`, `xVwa3`, `CYsxg7`, `MfOHt6`, `RlMmu2`, `RPKTR7`,
  `zYUn5`, `gsLi5`, `KnOhl5`, `uHNG9`): GPU identity, permissions, visibility,
  storage quota, network/media state and enumeration values belong to the
  selected Environment rather than captured constants.
- Timing/random/response/stack observations are explicitly classified in the
  registry. The two stable helper-cache branches (`HDEX5`, `Aqcaj8`) cross a
  measured five-second deadline; `KDQCx4` is the measured overrun watchdog.

Implemented semantic groups include structured clone and consumers, OPFS,
permission conversion, Trusted Types/CSP, Performance navigation/resource
ownership, CSSOM and computed style, Shadow DOM and foreign-realm geometry,
Canvas color/float/path state, SVG transform and text geometry, WebGL/WebGPU
software state, audio synthesis, media capability tasks, RTP/WebRTC and
image-metadata lifecycle. All models remain platform-neutral and avoid captured
hashes, host GPU/audio stacks and OS-backed storage.
