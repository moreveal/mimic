# Semantic packet from manual capture 211032

This report describes the packet after `7542e83`, with runtime and regression
sources committed through `8e62be3`. It supplements, rather than rewrites, the
[previous replay-scope correction](manual-replay-scope-2026-09-11.md) and
[cold-child snapshot report](bootstrap-cold-child-2026-09-11.md). Work was done
on the requested `main`; the starting tree was clean. No live target run was
performed. Private captures, VM sources, payloads and credentials remain ignored.
`a96d52e` subsequently adjusts only the three new regression-test watchdogs for
instrumented Goja bootstrap; production sources are unchanged.

The useful advance is a set of general browser fixes on the consumed execution
path, plus complete traversal of the three saved navigation cycles. It is not
proof that a new server will return `complete`. Saved responses do not evaluate
the newly generated request bodies. The remaining captured-data boundary is the
first request after the final saved response, fixture 38.

## Baselines and comparison rules

The source baseline is clean `7542e83`; its preceding runtime is `531e1e7`.
The baseline binary is `.build/mimic-snapshot-relations.exe`, SHA256
`771c9239897e69b328464d3369d298d23f7f63be0c306b88d8fc572436d4d21d`.
The final validation binary is `.build/causal-211032/mimic-packet-r4.exe`, SHA256
`af88569f9b64fea0c1b553f0c8fdf4fd909e257d60c89fa7dd8c90142a71e29f`.
Its recorded dirty source content is the committed packet, not just its embedded
revision. A subsequent clean build of `8e62be3`, with `vcs.modified=false`, is
`.build/causal-211032/mimic-clean-8e62be3.exe`, SHA256
`1ae87ef0b8271fa4911b38cfb8f0de0b0db0329632e0c2b5133c163e81cf2dcb`.

The reference is frozen **Chrome 152.0.7977.82**, Windows x64, headful, executable
SHA256 `ea36dd818a90176f1a70616f0363d9be527229389a6c073a0b1688b9e73f67e9`.
Focused fixtures used fresh profiles and independent Chrome contexts. Profile
names, arguments and actual viewport/security observations are retained in each
passport. A launch size is not substituted for an observed viewport: the general
control used hidden contexts with inner size 1272×653 and reported outer size
0×0. Loopback HTTP is not evidence of TLS/H2/H3 behavior.

The dedicated saved-program Chrome analysis used captured-response interception,
an outbound deny-all proxy and DNS/nonproxy-UDP restrictions. Dedicated-worker
Fetch interception is an explicit coverage gap. This diagnostic environment is
not claimed equivalent to an ordinary live browser session.

Exact probe and evidence hashes are in the [receipt](semantic-packet-211032-2026-09-11.json).
Every normalized observation leaf is compared; missing containers and execution
failures are recorded separately. Counts below are not summed across overlapping
corpora, repeated cycles or diagnostic representations. API trace access counts
are deduplicated and are not counts of JavaScript operations.

The old broad audit and the historical 236-check navigation fuzz baseline were
not rerun in this stage. Their observational groups are not fresh root-cause
counts. The user narrowed this stage to consumed values and causal progress;
unsupported discovery, image/capability expansion and performance experiments
were not substituted for that work.

## Confirmed corrections and localized defects

Priorities below concern local execution/consumed semantics, not a proven server
decision. `P1` means progress or widely consumed implemented behavior; `P2` means
an adjacent semantic integration correction. Rows group mechanisms, not leaf
counts, and do not assert a final number of independent native root causes.

| ID | Root cause or localized mechanism | Repro | Chrome | Mimic before → after | Priority | Status | Commit | Limits |
|---|---|---|---|---|---|---|---|---|
| H01 | Replay selected the first GET again after navigation | Saved fixture indices 0, 15, 30; offline replay unit tests | Separate saved documents per cycle | One cycle plus reload → all three cycles | P1 | Harness fixed | `827bcb0` | Critical-CH duplicate response and dynamic path substitution are explicit harness inferences |
| S01 | Document referrer lacked committed navigation state | `TestChildDocumentInheritsDomainAndNavigationReferrer` | Creator/initiator referrer subject to policy | Empty → measured initial, blank, srcdoc and network values | P1 | Semantic defect fixed | `6a5f728` | HTTP local oracle; does not establish all redirect/TLS policies |
| S02 | Domain read location instead of inherited origin | Same test, initial blank/srcdoc | Inherited creator hostname | Empty about URL hostname → inherited origin hostname | P1 | Semantic defect fixed | `6a5f728` | Not a new implementation of document.domain mutation |
| S03 | Artificial doctype in empty documents | `TestInitialEmptyDocumentHasNoDoctype` | Null doctype, BackCompat | Synthetic doctype/CSS1Compat → matching empty document | P1 | Semantic defect fixed | `6a5f728`, test `8d75d8f` | Network documents retain their parsed doctype |
| S04 | Isolation delegation was reduced to origin equality | `TestCrossOriginIsolationHonorsDelegation` | Container delegation bounded by parent/child policy | Incorrect blanket denial → matching tested allow/deny cases | P1 | Semantic defect fixed | `6a5f728` | COEP embedding rejection and complete origin-agent-cluster modeling remain separate |
| S05 | Resource timing used wall/bootstrap offsets and mutable ownership | `TestResourceTimingUsesInitiatingRealmClock` | Resource interval lies in owning document clock | Future/misattributed entry → immutable initiating owner/start | P1 | Semantic defect fixed | `29c5e4e` | Worker fetch does not leak into Window; full worker Performance support is not added |
| S06 | Cache reused original timing phases | Browser timing oracle; network cache unit test | New cache retrieval timing, cache delivery, no network protocol phases | Old response phases → new retrieval phases | P1 | Semantic defect fixed | `29c5e4e` | Transport identity is retained; CDP cache connection projection was not measured |
| S07 | Navigation metadata and serializer were disconnected | `TestPerformanceNavigationReasonAndResourceIdentity`; timing oracle | Reload type, working toJSON, resource navigationId matches owner | Incorrect type/serializer/correlation → matching tested relations | P1 | Semantic defect fixed | `29c5e4e` | No claim of universal navigation timing fidelity |
| S08 | Window handler attributes used publication storage | `TestWindowHandlerAttributesUseListenerState` | Null defaults, listener-position replacement, primitive conversion | Missing/default values and wrong dispatch model → shared event state | P1 | Semantic defect fixed | `8d75d8f` | Window.event invocation context is a different mechanism |
| S09 | Cross-realm call errors became generic host errors | `TestBorrowedCallsPreserveThrownValuesAndRealm` | Original thrown value and owning error realm | Generic error → preserved primitive/object/error identity | P1 | Semantic defect fixed | `5c53c8b` | Existing saved-eval access-gate restrictions remain |
| S10 | Outer WindowProxy Set and descriptor conversion lost receiver/value semantics | `TestWindowProxyWritesPreserveAccessorAndReceiver` | Accessor invocation and ordinary explicit-receiver rules | Rejected accessors/incorrect receiver → matching tested writes | P1 | Semantic defect fixed | `c322073` | Broad Window.set oracle still has preexisting replaceability/branding gaps |
| S11 | postMessage exposed a second callable and executed in the wrong shim realm | `TestPostMessageUsesOwningFunctionAndCallingDocument`; existing port tests | Owner callable identity, incumbent source, native callable shape | Split function/owner behavior → shared owner callable and caller dispatch | P1 | Semantic defect fixed | `fd024be` | Message serialization is not yet a full structured-clone codec |
| S12 | NodeFilter was a frozen plain object | `TestNodeFilterIsNonConstructibleInterface` | Extensible, nonconstructible interface function with readonly constants | Plain object → matching interface shape | P1 | Semantic defect fixed | `4109ad1` | Four exception-source projection observations remain in the broader oracle |
| S13 | Native iframe load omitted the embedding realm's job checkpoint | `TestNestedFrameOwnerLoadDrainsMicrotasks`; 11-case srcdoc matrix | Promise continuation progresses without an unrelated timer | 4/11 → 11/11 after-await reached | P1 | Semantic defect fixed | `282b695` | Checkpoints between individual native listeners remain unimplemented |
| S14 | Document.open cleared listeners but retained handler registration records | `TestDocumentStreamResetsHandlerAttributesAndRegistration` | Connected targets clear; detached targets retain; reassignment works | Stale getter and missing reassigned handler → matching complete oracle | P2 | Preexisting semantic defect fixed | `fc34dbc` | No direct saved-server-decision causality established |
| S15 | Restored bootstrap retained an eagerly seeded Window listener map | `TestBootstrapSnapshotNestedWindowMessageHandler`; warm shared corpus | Nested message relay completes | Intermittent lost listener lookup → lazy live-realm allocation passes | P1 | Localized browser defect fixed; lower native mechanism unproved | `8d75d8f` | Pure-engine WeakMap/snapshot reductions did not reproduce; not attributed to the historical native crash |

The Window setter and postMessage integration exposed intermediate failures,
including a port-delivery timeout and incorrect explicit receivers. They were
fixed before the final packet checks; they are not omitted from private evidence.
No probe-specific return values, target identifiers or native-globalThis wrapper
were added to production code.

## Before/after observations

| Fixed observation set | Before | After checkpoint | Control and interpretation |
|---|---:|---:|---|
| Repeated emitted property bucket, exact raw paths | 174 differing paths | 43 | Two repeated cycles; do not sum them |
| Same bucket, only dynamic widget path segment normalized | 173 | 41 | 132 formerly differing paths match; zero newly differing paths |
| Document inheritance focused oracle | 76 differing leaves | 0 | Chrome→Chrome 0 |
| Broad defaults/empty-document oracle | 3762 | 240 | Repeated profile/owner/remote observations, Chrome control 0 |
| Initial handlers oracle | 54 | 26 | Broader surface remains partial; Chrome control 0 |
| Isolation policy oracle | 83 | 52 | Tested isolation/SAB cases fixed; OAC/ready-state observations not promoted to new fixes |
| Fixed navigation/resource timing relations | 10 | 0 | Chrome control 0 |
| Independent delayed-navigation clock relations | 2 | 0 | Overlaps S05, Chrome control 0 |
| srcdoc owner progress | 4/11 reach after-await | 11/11 | Additional completed output exposes the already known between-listener ordering gap |
| Broad Window.set oracle | 149 differing paths | 91 | No newly differing paths versus baseline; not a complete pass |
| postMessage shape oracle | 49 | 7 | Remaining error/source projections; emitted postMessage observations match |
| Document.open handler lifecycle oracle | 6 R2 diff paths | 0 R4 | Frozen control 0; one diff path represented five missing handler calls |

The emitted property coverage is 1663 before, 1665 after, 1666 in Chrome per
bucket. Net minus one is not one missing property: Chrome has three native-only
emitted paths and Mimic two extra undefined-bucket paths. These are among the
retained divergences, not new coverage losses.

All ten instrumented serializer input/output pairs are complete, and all ten
outputs exactly match the emitted POST bodies. This establishes that those
values were consumed. It does not establish which value the server rejects.
The remaining 41 normalized paths are 13 Window state/publication observations,
20 capability objects, four per-frame viewport observations, one current-event
observation, and three environment/time observations. These are observational
categories, not 41 confirmed defects.

The unchanged general corpus contains 15 cases. The first packet matrix completed
13 and timed out in SameSite/partition after warmup. Cold r2 passed, but a warm
r2 sequence reproduced both the symptom and absent handler registration. A
synthetic event also failed; clearing and reassigning the same handler restored
delivery. Five focused repetitions passed after lazy allocation, followed by
the warm first-nine-plus-two corpus sequence on r3.

Final r4 completes **15/15**, with no coverage regressions and no changed
normalized observations against baseline in this general corpus. Existing
Chrome differences remain: network 12 leaves, surface 27 expanded leaves and
observations 23 expanded leaves. These sets are separate from the emitted data
above. The original Chrome A/B control differs only in ten HTTP connection reuse
ordinals; all its JS outputs match. Final r4 reuses that established control and
baseline after exact corpus/probe hash checks; it is a fresh Mimic after-check,
not another claimed fresh Chrome control.

## Offline execution, instability and remaining boundary

The occurrence-aware replay distinguishes transport fixtures, locally generated
blob/cache observations, recorded failures and unknown requests. It preserves
unused fixtures and stops without a network fallback. The defaults are bounded
to 60 seconds, 1600 scheduler advances and 256 transport requests. They are
diagnostic limits, not performance workloads.

One original uninstrumented r2 execution reached the step limit without all
submissions. Its exact repeat and the separately instrumented variants completed.
All three r3 variants completed. The failed execution is retained: a later pass
does not erase it. Static saved-source analysis shows a pending-execute flag;
the hypothesis that an early execute is simply discarded is unsupported. The
failed trace lacks values at the remaining guards and callback identity. Its
retrospective attribution to S15 is therefore unproved.

Final r4 uninstrumented, message-only and payload variants each complete all
three cycles, in 163/165/165 scheduler advances, exit 0 with empty stderr.
Each retains the five previous image-decode diagnostics and one scheduler
cancellation error after deliberate cancellation at the unknown-request boundary.
That cancellation is a harness stopping result, not an uncaught script failure.
Only favicon fixture 4 is unused because the runtime serves it from cache.
The complete results and process identities are recorded in the receipt.
The stop at the first uncaptured GET after fixture 38 is an actual saved-data
boundary. It is suitable for handing the accumulated packet back for the user's
next live run. It does not mean that all further offline semantic work is exhausted.

## Backlog and explicit boundaries

| Priority | Remaining issue/classification | Evidence and required work |
|---|---|---|
| P1 | Native callback microtask ordering: incomplete implementation | All 11 srcdoc cases retain a between-listener ordering difference; needs host callback cleanup checkpoints without adding them inside script dispatchEvent stacks |
| P1 | Window.event: incomplete realm/lifecycle state | Local control confirms callback-realm ownership, nested restoration and trusted-event microtask behavior; one Page-wide current-event variable is insufficient |
| P1 | Per-frame viewport: missing authoritative state | Local default/styled/hidden frame measurements require child viewport and resize/lifecycle state; Page viewport or a fixed zero is insufficient |
| P2 | Window scalar/replaceable state: incomplete implementation | Thirteen emitted observations plus retained Window.set differences; name/status/closed/opener/scroll/screen state needs owned browsing-context state |
| P2 | Cross-realm message serialization: architecture limit | JSON representation cannot preserve the complete structured-clone domain; copied-array identity alone does not prove a key-order cause for server behavior |
| P2 | Capability/media/GPU objects: unsupported/environment dependent | Twenty emitted objects are candidates, not proved blockers; controlled codec/renderer/device evidence required before implementation |
| P2 | Error/native function projections | NodeFilter and postMessage retain 4/7 focused error/source observations; classify against the exact operation before a further fix |
| Separate | Replay outlier: instability/unclassified | Keep failed r2 trace and exact build; future recurrence needs guard/callback values, not an inferred cause from a passing repeat |
| Separate | Strong bridge caches, old DOM arenas, retained closure retargeting, saved-eval call/apply/bind gate | Existing architecture limits remain; no new closure/global-proxy or lifetime completeness claim |

Snapshot native stability remains governed by the
[stability-debt disposition](snapshot-stability-debt-2026-09-11.md). The historical
SizeFromMap and other dumps/signatures are retained. S15 is a semantic lookup
failure, not a demonstrated member of that native crash family. One new native
crash reopens P0 immediately. No retry, global runtime lock or snapshot disabling
was introduced. No new fast gate or opt-in native stress matrix was performed;
the [performance/concurrency backlog](../performance/report.md) and its incomplete
results are unchanged. This packet makes no performance claim.

## Validation

Focused regression tests and affected local Chrome oracles were run during the
packet. Frozen expectations and the checked-in general corpus were not weakened.
The full browser/network/scheduler/CDP/engine/webapi suite was batched, not run
after every change. Its first execution passed browser and related packages but
found one stale private bootstrap test expecting eager Window topology. That
fixture was corrected in `8e62be3`; public topology expectations were unchanged.

The final full suite passes, exit 0: browser 385.328 s, network 0.300 s,
scheduler 0.407 s, CDP 2.349 s, Goja 0.194 s, QuickJS 0.256 s, V8 1.214 s,
webapi 0.266 s and offline replay 0.139 s. The engine interface package has no
test files. These durations identify the executions and are not benchmarks.
Nonverbose output does not provide a total skip count; opt-in stress and
performance workloads were not enabled.

The targeted browser race batch records 65 pass and six fail events (three test
families and their Goja children), zero skips and no data-race warnings. All
failures are the eight-second evaluation watchdog expiring during multiple Goja
realm bootstraps. An isolated control preserves two of those deadline failures,
so they are not attributed only to parallel load. The three new tests now have
a 90-second diagnostic watchdog; no semantic assertion or production timeout
changed. All three families, Goja and V8, then pass under race: nine pass events,
zero fail/skip events, 61.404 s. Previously passing families were not repeated.
This is the combined targeted coverage, not a claim that the original failed
invocation passed or that a full browser race suite ran.

Network, scheduler, CDP and V8 race packages pass with 184 pass events and two
explicit opt-in skips (`TestOwnerModelProbe`, `TestHostBoundaryProfile`), no
data-race warnings. Test/subtest events are reported separately from independent
test counts. The exact logs and failures are hashed in the receipt.

Python differential unit tests pass 7/7 from `tools/compatibility`; an earlier
repository-root invocation failed its local module import and is retained as
a command setup error. Ignored private-capture Go diagnostics were not treated
as project packages. The full suite precedes only the test-watchdog adjustment;
all production source hashes still match the tested r4 build.

Preserved nonsemantic failures include the unstable optional timer in one Chrome
srcdoc control, HTTP fixture connection resets during teardown, and a Python
daemon-thread shutdown failure after one diagnostic capture. Browser process
status is recorded separately from intentional termination. None of these is
silently converted into a Chrome semantic mismatch or a native browser crash.
