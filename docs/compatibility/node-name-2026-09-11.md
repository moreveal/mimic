# Node name ownership and private-state dispatch

Baseline production behavior is bda67db; the isolated worktree starts at
cee1376, which adds only snapshot investigation documentation. This follows
the [Attr/Node binding correction](attr-node-2026-09-11.md).

| ID | Root cause | Repro | Chrome | Mimic before | Priority | Status | Fix | Limits |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| NODE-NAME | Exposure relocation replaces Node's general getter with Element's public tagName lookup | node_name_oracle.js | Node getter selects the receiver's private state | Undefined for legitimate non-Element receivers; public tagName shadowing and prototype changes alter the result | P1 | Corrected; joint package validated | Dispatch from private node kind and qualified name | Other inherited Node operations and cross-realm dispatch are separate work |
| NODE-NAME-PLACEMENT | Late specialization restores extra own nodeName descriptors | Same oracle | CharacterData and DocumentFragment inherit Node's getter | Extra own descriptors hide the broken inherited binding | P1 | Corrected; joint package validated | Remove both early and late redundant specializations | Does not change generic descriptor relocation for other members |

The expanded focused probe improves **19 differing leaves to zero**, with
zero Chrome-to-Chrome control differences. This is two related implementation
findings, not nineteen independent root causes. The initial discovery probe had
seven differing leaves; it is a separate smaller set, not the before count of
the expanded probe. Text, Comment, Element, DocumentFragment, DocumentType,
Document, ShadowRoot, SVG and Attr are exercised. Public tagName shadowing and
prototype replacement must preserve the borrowed getter's result.

The first discovery attempt aborted because Mimic does not implement
DOMImplementation.createDocumentType. The completed probe uses the fixture's
existing document.doctype; the incomplete attempt is not counted as a semantic
match or mismatch. An intermediate implementation failed the SVG qualified-name
case in both ordinary and snapshot tests. Reading the existing private
qualifiedName instead of the uppercase fallback corrects that failure.

Focused Node-name and existing Attr tests pass in ordinary and restored snapshot
modes (1.390 seconds). The final combined semantic build retains 205/236 fixed
matches and 19/28 original representatives, with zero changed observation
leaves and zero Chrome drift. Corpus receipts for the joint batch are in the
[exception follow-up](domexception-state-2026-09-11.md), including the completed
full browser suite, targeted race and complete fast gate. Performance results
are mixed; broader compatibility cleanup and snapshot P0 remain open.

[Evidence](node-name-20260911/) records normalized observations, binary and probe
hashes, worktree identity and Chrome metadata. Frozen Chrome 152.0.7977.82 runs
headful with the reused controlled profile. Capture helper freshness metadata
is corrected to unverified-reused-controlled without altering expectations.
Only local fixtures ran. The [historical snapshot P0](snapshot-sizefrommap-2026-09-11.md)
remains a separate open investigation.
