# Compatibility cleanup: interface inheritance, 2026-09-11

This is one completed semantic package within the requested broad cleanup, not
completion of that cleanup. No target website was launched. Historical audits
remain unchanged. The baseline was clean `037f8ff0e1146bab8f531a758cc59d6291c9b82b`
on main, after `b94c63c`. The reflection/global-order/iterator package was already
committed as `037f8ff`. Work uses `codex/compat-cleanup-20260911`; no preexisting
uncommitted changes were present or included.

## Evidence and comparison rules

Local receipts are in `compatibility/private-captures/cleanup-20260911/`.
`identity.json` records the clean starting revision, executable SHA256, Chrome
SHA256, owned process IDs and launch arguments. Chrome is frozen
152.0.7977.82, Windows x64, headful, a newly created controlled profile,
1280x800, no feature overrides. The profile is reused within this investigation;
individual probes use fresh targets. The headless sweep control has a separate
new profile and is supplementary, not an authoritative replacement.

Baseline executable SHA256:
`bc2a0988e8a6ed96e1af08a9dfe098f0f945ea2de366a72583d568bd74af3dcd`.
The final tested executable identity and source/probe hashes are retained in the
companion results JSON and private receipts. Private raw captures are not tracked.

The original 236 probe definitions were recovered from the retained fuzz worktree
(`.build/fixes-block1-observations.json`). The exact list was replayed before and
after, with all normalized observations compared, alongside all 28 original
minimal representatives. Both sides use the same rebound loopback origin.
Discovery was not used to remove or replace any original probe. Supplemental
navigation, inheritance, brand, relation and sweep results are separate corpora;
their overlapping counts must not be added into a compatibility percentage.
Array-length differences retain the full arrays, as in the existing typed diff.

| Fixed set | Before | After | Chrome control |
|---|---:|---:|---:|
| Original probes, full match | 135/236 | 138/236 | 236/236 |
| Original representatives, full match | 9/28 | 10/28 | 28/28 |
| Remaining observational signatures | 19 | 18 | 0 |
| Previously matching probes regressed | вЂ” | 0 | вЂ” |
| New differing paths / Chrome oracle drift | вЂ” | 0 / 0 | вЂ” |
| General differential corpus | 12/15 | 12/15 | 15/15 |
| General corpus differing records | 41 | 41 | 0 |
| State relations differing records | 4 | 4 | 0 |
| Brand matrix differing records | 1351 | 1351 | 0 |
| Standalone sweep matches | 112/130 | 113/130 | see mode note below |

The signature reduction is one original observational group, not 19 proven causes
reduced to 18. Constructor inheritance and the additional DOMException constructor
chain are two confirmed corrections. The brand matrix's 1351 JSON diff records
are not counts of operations or independent bugs. The general corpus still differs
in network (12), surface (7), and observations (22). Redirect/navigation cookies,
CORS rejection and history copying remain matched in their existing probes.
Loopback HTTP/1.1 says nothing about TLS/H2/H3.

The sweep executes 130 attempts after its existing safety/fixture exclusions;
the original historical audit's 134 attempts included unavailable fixtures.
Before and after use the same current manifest and expression hashes. The one
changed path is `probes-prefo:4` (HTMLSpanElement interface-object own keys).
Timing/profile observations remain separately classified. Chrome headful versus
headless is a mode control, not a Chromeв†’Chrome headful stability proof; the fixed
corpus, general corpus, brand, relations and navigation have headful controls.

Semantic fix commit: `6f422ba` (SSH-signed, with sign-off).

## Implementation

Generated WebIDL installation now links interface objects to their parent
interface objects after all exposed declarations exist. It already linked
instance prototypes; static inheritance and reflection now agree with that chain
and remain realm-local. This closes original group `7ce0490158dc72b8` and three
complete fixed probes. It does not add APIs or alter unsupported method bodies.

DOMException now constructs its Error-backed instance independently of its public
constructor chain. Its prototype still inherits Error.prototype, while the
interface object inherits Function.prototype, as measured in Chrome.

The broad sweep caught an intermediate regression: XMLHttpRequest's handwritten
constructor skipped XMLHttpRequestEventTarget, so installing the correct public
chain routed super() into an unsupported generated constructor. A real intermediate
interface now performs internal EventTarget initialization; its public constructor
remains illegal. The new oracle covers XHR creation/brand/state as well as main,
child and restored-realm inheritance. The failed intermediate sweep and aborted
superseded full test run remain in the receipts; neither is a final pass.

The generated artifact manifest also lacked the four order artifacts and carried
the stale exposure-data hash from `037f8ff`. It was refreshed against the existing
tracked artifacts plus this regenerated surface; no order expectation changed.

## Updated triage

| ID | Root cause or hypothesis | Repro | Chrome | Mimic | Priority | Status | Fix / commit | Limits |
|---|---|---|---|---|---|---|---|---|
| C01 | Missing interface-object inheritance | original `7ce0490158dc72b8`; interface_inheritance oracle | parent interface | Function.prototype on generated derived interfaces | P1 | fixed | 6f422ba | No new capability implementation |
| C02 | DOMException constructor uses JS Error subclass chain | interface_inheritance oracle | Function.prototype | Error before fix | P1 | fixed | 6f422ba | Other DOMException conversion/code issues not claimed fixed |
| R01 | Native global-proxy retargeting absent | navigation_realm_ownership | replacement global / SecurityError | old global, two leaves | P1 | architecture-blocked | open | Requires engine design; no globalThis JS wrapper |
| R02 | Delegated saved-eval bypass | navigation_window_edges | undefined | 17 via call/apply/bind | P1 | architecture-blocked | open | Direct gate passing is insufficient |
| R03 | srcdoc navigation missing | state-relations; navigation_nested_window | script creates nested Window | missing setup, five consequential leaves | P2 | incomplete implementation | open | Not five lifetime bugs |
| R04 | Borrowed HTMLAllCollection receiver is owner-local | state-relations | item succeeds | Illegal invocation | P1 | confirmed semantic defect | open | Native HTMLDDA must remain intact |
| R05 | Imported HTMLDDA prototype is fixed at import | state-relations | live changed prototype | stale prototype; property read works | P1 | architecture-limited | open | Native interceptor cannot trap getPrototypeOf |
| R06 | Strong bridge caches / old DOM arena nodes | ownership documentation and source boundary | weak reachability | conservative retention | P1 | architecture-limited | open | No new reclamation proof from realm-count tests |
| R07 | Cross-origin Window reflection | navigation_cross_window_reflection | frozen observations | all match | P1 | superseded by 037f8ff | 037f8ff | Not complete cross-origin method parity |
| W01 | Receiver/arity/conversion ordering | original 4c01/4d8f/76be/90dd groups; brand matrix | WebIDL rejection ordering | differing exceptions/results | P1 | confirmed semantic defects | open | Implemented operations first |
| W02 | Callable shape finalized before later replacements | original 21a8/c5a8/a6c9 groups | native source/IDL lengths | JS source/internal lengths | P2 | confirmed semantic defects | open | Preserve identity and nonconstructibility |
| W03 | Window named access and Location unforgeables | original 931e/56e9/98f3/d735/f5bf groups | named descriptors / readonly methods | missing or different descriptors | P1 | confirmed semantic defects | open | Canonical collection identity already fixed |
| W04 | Interface member order / inventory | original 2e3f/2f13/91b5/a99b/a9a6 groups | Blink order | remaining mismatches | P2 | mixed surface limitations | open | Window publication fix is not all interface ordering |
| W05 | hasFocus unsupported | original babc group | boolean | undefined | P3 | incomplete implementation | open | Requires focus/active-document state |
| N01 | Header / CDP metadata projections | general network corpus | retained values | 12 records differ | P2 | wire/projection triage | open | HTTP/1.1 only; raw headers retained separately |
| E01 | Timing/network estimates and graphics/text metrics | sweep; observations corpus | environment-scoped values | differing model values | P2 | environment-sensitive / limited model | open | No fingerprint constants or exact duration requirements |
| P01 | Intermittent native snapshot failure | lifecycle stress | n/a | multiple historical signatures | P0 | unresolved | separate snapshot report | Passing bounded repeats do not prove absence |

## Validation and remaining scope

Final receipts are linked from [results](cleanup-20260911/results.json) and
[corpus summary](cleanup-20260911/corpus-summary.json). The exact
[fixed definitions](cleanup-20260911/fixed-probes.json) are retained independently
of future discovery. SHA256 values hash canonical sorted JSON probe definitions.

- Full final browser suite: PASS, 264.955 s. Webapi, scheduler, DOM, network, CDP,
  and Goja/QuickJS/V8 engine packages: PASS. Engine contract package has no tests.
  [Log](cleanup-20260911/final-full-tests.txt).
- Targeted browser race (inheritance, Window reflection, iterator cache and
  document.all frozen oracles): PASS, 8.462 s.
  [Log](cleanup-20260911/targeted-race.txt).
- Complete webapi/scheduler/network/CDP/V8 race packages: PASS.
  [Log](cleanup-20260911/packages-race.txt). Full browser race was not run;
  historical full-race deadlines are not declared resolved.
- Python fuzz harness: 21/21 PASS with live Chrome, no skips.
  [Log](cleanup-20260911/harness-live.txt). Earlier offline invocation passed
  with six opt-in live skips. Differential helper tests: 7/7 PASS.
- Offline generated-data and retained-oracle checks: PASS. The initial generated
  manifest check failed on inherited stale metadata, corrected as described above.
- Official final discovery run: 236 checks, 138 matches, 98 divergences,
  18 groups, zero unstable/errors; 3533 discovered probes, unchanged limit 100.
- Five supplemental navigation oracles have no headful-control differences.
  [All observations](cleanup-20260911/navigation-final.json) preserve the
  retargeting/eval/srcdoc failures rather than filtering their unsupported fields.
- Snapshot lifecycle controls: six processes PASS; the subsequent clean-baseline
  fast gate FAILED with native access violation, exit 2, on a 10-Page wave.
  Final fast gate PASS, including all mandatory workloads, 10/25 waves and memory.
  This is not a clean passing pair or proof of snapshot repair. See the
  [separate P0 report](snapshot-cleanup-control-2026-09-11.md).
- Initial fast-gate launcher could not import websockets.sync from the old fuzz
  venv; no workload ran. The installed system Python has the required benchmark
  transport and executed the recorded gates. No dependencies were changed.
- Intermediate inheritance-only build failed two sweep probes due to XHR
  construction. Its full suite was stopped as superseded; it is not a pass.
  Final source passed the complete suite and replays after the structural fix.

The fixed and supplemental corpora are not an exhaustive Web Platform conformance suite.
The historical 127 extended audit scenarios, broader worker/observer scheduling,
inactive-document capability sources, cross-realm newTarget, CSS/layout, media,
WebRTC, and remaining unsupported APIs were not exhaustively re-audited here.
They remain backlog rather than inferred fixes. No site outcome is inferred.
