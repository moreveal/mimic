# Consumed residual packet, capture 225523

Historical checkpoint: the nine service gaps below are addressed by the subsequent
service implementation packet; this document preserves the earlier 41-to-11 receipt.

Baseline: `afff4ec`. Input: the user's `manual-20260911-225523`. The original
residual was **41 normalized consumed property paths**, approximately 42 in the
work order. This packet removes **30**, leaving **11: nine unsupported service
objects and two execution/environment observations**. Nothing is silently
normalized away or replaced with an empty capability object.

The new capture's fixed native reference A initially differs on **47** paths
and finally on **17**, with **zero newly differing paths**. Its six additional
paths are Document visibility (four aliases) and outer Window dimensions (two).
All six also differ between two executions of the **same saved programs in
Chrome**, without the diagnostic getter wrappers. They are retained in the
17-path ledger, separately from the original residual.

This is not a claim of absolute zero compatibility differences. It is a
disposition of every remaining consumed path: the existing semantic mechanisms
have no retained identified mismatch in this set; nine absent capabilities
still need service implementations. The boundaries below explain why publishing
their objects alone would not be a valid fix. They are not claimed fundamentally
impossible or automatically equivalent to GPU limitations.

Exact hashes, every retained leaf and its evidence are in
[the receipt](residual-225523-2026-09-11.json). Private sources, bodies, tokens,
profiles and traces remain ignored under `.build/residual-225523` and the
supplied capture directory. No live target request was made.

## Semantic corrections

| Mechanism | Result and focused evidence |
| --- | --- |
| Window state publication | `name` belongs to the browsing context; `status` belongs to its current Window/Realm. Frozen navigation control confirms name survives and status resets. Closed state reads the active frame registry. Opener, initial scroll aliases, offscreen buffering and screen-position aliases have their measured values; screen positions read Environment. Replaceable accessors install an ordinary own data property on assignment. |
| Frame viewport | Child getters read their embedding element's existing CSS/box model, not the Page viewport. Default, explicit, percentage, initially hidden, subsequently hidden and visible-again cases use the existing measured oracle. Previously observed dimensions survive `display:none`; detached browsing contexts have zero inner and outer dimensions. No rendering/GPU backend is introduced. |
| CSS style forwarding | `iframe.style = "display:none"` now forwards to `style.cssText`. The missing setter was the reason the consumed clean iframe remained 300x150. Native saved-program diagnostics observed the hidden frame; the local string-assignment oracle reproduces the defect without saved program code. |
| Private stylesheet query | Internal style enumeration now uses the private selector implementation. Calling the author's overridable `querySelectorAll('style,link')` introduced a new consumed side effect during viewport computation. Replay caught it; the final comparison has no new differences. |
| Window.event | Listener invocation pushes/restores current event in the callback object's realm. Function listeners use the function realm; object listeners use the listener object's realm, including a foreign `handleEvent`. Shadow-tree suppression and nested restoration match the retained Chrome oracle. |
| Native event callback cleanup | Production V8 drains jobs after each native listener returns, before restoring current event. Script dispatch stays synchronous. An explicit user-script depth guard prevents the private native dispatch stack from creating a checkpoint inside an outer script/timer. The complete local event oracle is 0 differing leaves; a separate regression distinguishes between-listener native checkpoints from script dispatch. |
| Small Window objects | Six distinct BarProp objects, legacy External no-op methods, null fence, and StyleMedia use real branded behavior. VisualViewport metrics and Viewport segments project the same viewport state. Segments are fresh frozen arrays of fresh DOMRects for the top document and null in children, as measured. Objects are gated by the selected bundle's exposure. |
| CPU performance | `navigator.cpuPerformance` is a runtime-enabled profile projection. The local frozen control reports class 4 on the selected 28-processor machine. The value and its availability are explicit Environment fields; the getter does not hardcode 4. A different configured class is tested. Unmeasured profiles do not fabricate a class. |
| Replay missing-body handling | Capture fixture 36 is a synthetic worker response whose CDP body retrieval failed. It is now explicitly reported as unavailable, never mistaken for an empty transport response. Missing non-synthetic bodies fail capture loading. Both cases have focused tests. |

Scroll commands retain their existing unsupported boundary; exposing the
document's initial offsets does not add a second approximate scrolling engine.
Visual viewport metrics cover the current non-zoomed, single-display model;
pinch zoom, folding devices and complete resize/render scheduling are not
claimed. Goja cannot drain its job queue inside the private native callback
stack. The full native-event oracle and between-listener test explicitly skip
that alternative engine; the scalar, lifecycle and object tests run on both
Goja and V8. This is an engine boundary, not a claim that Goja matches that
native ordering.

## Every unsupported service leaf

The consumed observation for each row is Chrome object versus Mimic undefined.
The generated prototype surface does not supply an implementation: methods
still reach unsupported publication/binding paths. None is classified as a
renderer requirement merely because it is not implemented.

| Remaining leaf | Concrete package boundary |
| --- | --- |
| `caches` | No origin-owned CacheStorage service. The loader's HTTP cache has different ownership and matching semantics. Request/Response snapshots, Vary matching, atomic operations and cross-realm sharing must be implemented together. |
| `cookieStore` | The authoritative HTTP cookie jar exists, but structured CookieStore queries and subscriptions do not. Correct change events require integrating document, HTTP and CDP mutations across contexts, plus CookieStore URL/domain validation. A separate JS cookie map would violate the state model. |
| `indexedDB` | No transaction/object-store engine, ordered key/range model, clone persistence, or versionchange/blocked lifecycle exists. A factory object without these contracts would be superficial coverage. |
| `navigation` | Existing History state is not the Navigation API's entry/transition/interception contract. Entry identity, commit/abort, traversal and navigate-event integration require a new complete projection over navigation ownership. |
| `scheduler` | Existing Page task queues do not implement prioritized postTask/yield continuations or TaskSignal reprioritization/cancellation. Substituting timers would introduce incorrect ordering. |
| `crashReport` | No crash-annotation sink or crash-reporter ownership contract exists. Accepting annotations into an unconsumed JS object would discard their effect. |
| `launchQueue` | No application-launch event source, consumer queue or file-handle delivery integration exists. A no-op consumer registration would advertise an absent capability. |
| `documentPictureInPicture` | No activation-driven PiP document/window service, presentation surface or PiP close/ownership lifecycle exists. This is a window/presentation subsystem boundary. |
| `speechSynthesis` | No OS voice registry, utterance queue, speaking/pause/cancel state or speech event source exists. Fake empty voices would not establish compatibility. |

Closing these nine rows honestly requires new capability/service work, not
another scalar/prototype patch. They are explicitly retained as unsupported
capability differences; the report does not count them as implemented APIs or
as proven server-decision causes.

## Every environment/control leaf

| Remaining leaf(s) | Evidence and classification |
| --- | --- |
| `d.lastModified` | Separate offline executions have different wall-clock times. The fixed local Chrome/Mimic control has the same date string as its run clock when no Last-Modified is supplied. No captured timestamp is substituted into production. |
| `n.webdriver` | Native offline launch uses `--remote-debugging-port=0`. The fixed-port local Chrome control reports false, matching Mimic. The offline native reference reports true. This is a diagnostic launch difference. |
| `d.hidden` | Changes between unmodified saved-program Chrome A and B; local fixed control matches Mimic. |
| `d.visibilityState` | Same independently retained Chrome A/B presentation difference. |
| `d.webkitHidden` | Same state through its legacy alias; retained as its own consumed leaf. |
| `d.webkitVisibilityState` | Same state through its legacy alias; retained as its own consumed leaf. |
| `outerWidth` | Chrome A/B changes between 0 and the observed 1280 outer width. The fixed local control matches the configured Mimic outer size. |
| `outerHeight` | Chrome A/B changes between 0 and the observed 800 outer height. The fixed local control matches Mimic. |

Chrome A/B additionally changes four screen-position aliases. Those are not
additional retained mismatches against the fixed reference A; Environment
already supplies their coordinates. The diagnostic getter-wrapper execution
was used only to localize style/viewport behavior, not as the comparison oracle.
An early hypothesis attributed the extra visibility/outer values to detach;
local detach tests confirmed a real lifecycle correction, but unmodified
Chrome A/B established the final classification of these consumed leaves.

## Replay and validation

Both repeated consumed buckets contain **1666** final observations, equal to
Chrome (baseline 1665). The two repeats each show 47 to 17, or 41 to 11 for the
original set. Repeated cycles are not summed. Only the previously documented
dynamic route segment is normalized. All ten serializer input/output pairs
are complete and all ten outputs match the emitted POST bodies.

Final original and payload replay use the same freshly built binary and
complete three cycles in 164 and 163 advances. They stop at the first request
outside the saved exchange, with the recorded cancellation diagnostic. The
payload run leaves the cached favicon fixture unused; the original run uses
all available fixtures. Synthetic body 36 is explicitly unavailable in both.
Stored responses cannot evaluate fresh submissions, so none of this establishes
live server acceptance.

Both final replay processes exit 0. Native reference A retains one
`Fetch.failRequest` error for an already-destroyed CDP session; reference B has
zero errors. Both supply all ten complete consumed serializer pairs. The
native interception runner's existing dedicated-worker coverage limitation
remains; its deny-all proxy is not replaced with a live transport fallback.

The first full package run passed the non-browser packages and found three
browser test/subtest failures: the new small-object publication assumed BarProp
existed even in deliberately minimal custom bundles. Exposure guards fixed the
implementation; no test or frozen expectation was weakened. The subsequent
full browser run passes. Final source validation, targeted race checks and the
retained failed invocation are identified in the receipt. No native crash was
observed, no retry/global runtime lock was introduced, and no performance claim
is made.

The final CPU/profile packet passes browser (244.856 s), state, Chrome 152,
Web API and replay packages. The remaining network, scheduler, CDP and all
engine packages pass on the final source. Targeted browser race batches pass,
including the added CPU projection and native-listener checkpoint tests;
network/scheduler/CDP/V8 race packages also pass. This is targeted race coverage,
not a full browser race suite. The earlier 250.596 s passing browser run and the
251.323 s failed invocation are both retained; durations identify runs and are
not performance measurements.
