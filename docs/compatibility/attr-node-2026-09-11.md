# Attr inheritance and borrowed Node bindings

Baseline 27d51d9, following [Document setters](document-setters-2026-09-11.md).

| ID | Root cause | Repro | Chrome | Mimic before | Priority | Status | Fix | Limits |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ATTR-NODE | Attr specializations were own properties and inaccessible through borrowed Node bindings | attr_node_oracle.js | Inherited Node descriptors read/write/clone private Attr state | Extra own descriptors, wrong values, exceptions on legitimate borrowing | P1 | Corrected for seven accessors and cloneNode | Node bindings select private Attr slots; remove overriding Attr members | Other Node specializations and cross-realm Attr ownership remain separate work |
| NODE-BRAND | Existing Node getters lacked private receiver validation | Same oracle | TypeError for invalid receiver | Defaults or undefined values | P1 | Corrected for affected accessors and cloneNode | Validate isDOMNode before access | Not a claim that every Node operation has correct conversion/receiver semantics |
| ATTR-STRING | Attr value conversion accepted Symbol | Same oracle through Node setters | TypeError | Accepted or ignored input | P1 | Corrected | Shared WebIDL string conversion | Other attribute APIs are not newly validated |

Focused normalized leaves improve **24 to zero**, with zero focused Chrome control
differences. The oracle checks placement, borrowed read/write/clone, private slots
after prototype replacement, attached attribute value shadowing, invalid receivers
and Symbol conversion. Original fixed corpus stays 205/236 and representatives
19/28, without new differing leaves, regressions or Chrome drift. Brand corpus
improves 578 to 558 records, twenty removed with no added/changed records; general
and relations remain 40 and two. Overlapping sets and leaves are not summed as bugs.
[Evidence](attr-node-20260911/) records exact observations and transitions.

An initial discovery fixture did not catch a failing baseline setter and aborted;
that incomplete evaluation was not counted. The completed probe records each
operation exception. No frozen expectation was weakened.

The capture helper labels profiles fresh-controlled without checking freshness.
This package explicitly records the reused controlled profile as unverified-reused-controlled
and corrects that metadata in the preceding Document setter capture and receipt.
No Chrome observation or expectation changed. All probes use local fixtures,
frozen Chrome 152.0.7977.82 headful, and the same dedicated profile; no target site ran.
Full unchanged Chrome corpus control, discovery and sweep are not repeated here.

P0 remains separate: see [snapshot serialization](snapshot-serialization-2026-09-11.md)
and the [historical SizeFromMap read](snapshot-sizefrommap-2026-09-11.md).
Finite passing tests cannot attribute or close the unreduced historical signature.

Full browser suite PASS (392.946 s output / 401.042 s completion); engine V8/Goja,
CDP and WebAPI pass. Focused tests PASS (3.706 s); targeted browser race PASS
(6.995 s); filtered WebAPI race has no matching tests. Existing skips are retained.

Both complete monitored fast gates pass with no native dumps. Initial invocations
failed during Python imports (websockets.sync missing), before build/workloads;
these infrastructure failures are retained. Corrected invocations use the existing
benchmark venv, websockets 13.1. Baseline production source is 27d51d9; its two
untracked diagnostic test files are excluded from the executable build.
Throughput at 10/25 Pages is 74.33/83.15 before and 70.98/63.14 after sessions/s.
DOM execution/completion 457.02/482.05 to 452.40/484.04 ms; React 52.15/84.11
to 58.37/93.03 ms. This mixed pair leaves a concurrency/performance concern open;
no neutrality or complete causal attribution is claimed. Every gate workload and
teardown wave executed; no failed workload was excluded or retried.

Static/React private memory after recovery is 156.75/166.61 to 157.24/170.47 MiB.
