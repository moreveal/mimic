# HTML form values and the current React boundary

2026-09-09. Independent reproduction in before.json: `input.value` without an attribute returned null instead of an empty string; `defaultValue/checked/defaultChecked` also returned null. Assigning value was ignored by a generated stub. Dirty-value/default-value separation did not exist.

This confirmed the current GitHub blocker through its stack, not just an API name: the FilesSearchBox layout effect compares DOM input.value with query="" and calls the setter with the DOM value. Null entered React query state, then FileResultsList evaluated query.length and failed. Stack: zy-c855f0bf7926778a.js:2:2500. The independent reproduction exercises the same transition without GitHub or React.

A separate CDP bug was also found: DOM.getOuterHTML returned the original d.Source, so previous post-hydration DOM snapshots did not measure mutations. Actual documentElement.innerHTML and Mimic.captureSnapshot showed Skeleton 0, but also the error fallback 'Unable to load page'. Disappearance of the skeleton alone is not success. The CDP fix is separate.

form_controls.js implements a coherent value-state layer over canonical element identity and attributes: input modes/defaultValue/dirty value/checked, checkbox/radio; textarea direct-child text/default/dirty value; select/option selectedness/value/index/options; reset and form association; text-selection APIs. WeakMap stores contain only live IDL state, not a second copy of the DOM.

A fresh forms-profile-before was captured before changes (DOM/static/React native 1 PASS). Generic results after changes: 6 groups match Chrome 152 completely, plus the original 3 reproduction groups. WPT files were unchanged; revision and hashes are in the manifest. Tests ran in Window with ordinary HTML loading, testharness, and a collector instead of visual testharnessreport.

Five WPT files: 71/71 assertions PASS in Chrome 152 and Mimic iteration 9 with form_controls.js injected through Page.addScriptToEvaluateOnNewDocument. This was not the final compiled integration; its embedded checkpoint is recorded below. Counts: valueMode 40, input-value-invalidstateerr 1, select-value 4, selected-index 14, textarea value/defaultValue/textContent 12. A sixth file, email-set-value, requires test_driver, absent from this limited runner: 1 infrastructure failure on both sides, excluded from the pass count. These results do not represent complete forms WPT coverage.

Limitations: month/week validation, date/time valueAsNumber/valueAsDate/step, range step rounding, constraint validation/submission/FormData, complete file lists, and control rendering remain unfinished. Type/attribute transitions through direct setAttribute need a broader corpus. Selection-event timing and form controls inside ShadowRoot also require separate comparisons. Current fixes contain no GitHub-specific behavior.

Additional testing of the mature Streams implementation exposed an incorrect old smoke test: awaiting writer.write before the first TransformStream read also hangs in Chrome because of backpressure. The benchmark was unchanged; the regular TestReadableWritableAndTransformStreams was corrected to read concurrently while retaining result validation. TestTimerAndFetchUseScheduler was not weakened: its empty bytes were traced to a generated structuredClone stub called by the Goja fallback polyfill, requiring a fix at the integration boundary.

Final embedded checkpoint: 71/71 PASS, all 5 harnesses completed, without production JS injection. Binary SHA-256 `F58FFF2043AB801F0BF608F8626BD981C38848AA1FC976A7A3CA6F410B878F24`; endpoint 19546 was verified against PID 27400 and the executable path. The runner checked source bytes against immutable WPT revision `b46e71b2f22c73fbcecaf26009e3082fddbaa744` and manifest hashes. [Full receipt](final-embedded.json). Chrome 71/71 is the earlier oracle for the same manifest files; no new run overlapped the isolated performance window. This checkpoint precedes the separate restoration of the internal Find lookup, which does not change public DOM selectors.
